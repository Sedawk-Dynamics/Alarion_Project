// Route entry for "/auth" — re-exports the auth UI from components/routes/auth.
// Keeps all real code in components/; app/ only owns the URL.
export { default } from "@/components/routes/destinations/page";
