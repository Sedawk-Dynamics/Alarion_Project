// Route entry for "/experiences" — re-exports the UI from components/routes/experiences.
// Keeps all real code in components/; app/ only owns the URL.
export { default } from "@/components/routes/experiences/page";
